<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hu.bme.aut.hazi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/sun"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/sun"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hazi.NoActionBar">
        <activity
            android:name=".LoadActvity"
            android:exported="true"
            android:theme="@style/LoadTheme">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>/></activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/LoadTheme">

            <!-- android:theme="@style/LoadTheme"> -->
            <!-- android:name=".MainActivity"

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>-->
        </activity>
        <activity
            android:name=".ChooseActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/LoadTheme">
        </activity>
        <activity
            android:name=".EtkezesActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/LoadTheme">
        </activity>
        <activity
            android:name=".FavouritesActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/LoadTheme">
        </activity>
        <activity
            android:name=".PlusActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/LoadTheme2">
        </activity>
    </application>

</manifest>